import 'package:flutter/material.dart';

class AkomodasiWidget extends StatelessWidget {
  const AkomodasiWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Konten Akomodasi',
        style: TextStyle(fontSize: 16),
      ),
    );
  }
}
